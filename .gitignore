# Django / Python
__pycache__/
*.py[cod]
.venv/
venv/
*.log

# Local database (rebuilt from migrations + seed_demo — see ADR-002)
db.sqlite3
db.sqlite3-journal

# Environment
.env

# Vendored front-end assets ARE committed on purpose (T-26): the demo must not
# depend on venue wifi. Do not add core/static/vendor/ here.

# collectstatic output — a build artefact, rebuilt by build.sh on every deploy.
staticfiles/

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

VIDEO-SCRIPT.md
