# DealFlow360 — ADR-001 (Django 5) and ADR-002 (SQLite).
# Deliberately tiny: HTMX and Tailwind are CDN <script> tags, not Python packages.

Django==5.2.17
python-dotenv==1.2.3

# Deployment only (ADR-018). Neither is imported by application code; settings.py
# guards the whitenoise import so a local venv without them still boots.
gunicorn==23.0.0
whitenoise==6.8.2
